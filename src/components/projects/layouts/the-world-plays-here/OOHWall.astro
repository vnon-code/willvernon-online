---
/**
 * the-world-plays-here — Urban Out-of-Home Campaigns (step 04). The legacy stage + thumbnail-switcher
 * mockup, redrawn as a ruled stage-and-wall grid: see MockupWall.astro.
 */
import type { MediaItem } from '../../../../content';
import MockupWall from './MockupWall.astro';

interface Props {
  media: MediaItem;
  slug: string;
  group: string;
}

const { media, slug, group } = Astro.props;
---

<MockupWall media={media} slug={slug} group={group} />
